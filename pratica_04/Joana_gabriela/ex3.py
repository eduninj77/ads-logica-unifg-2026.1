palavra = "algoritmo"
notas = [7.0, 8.5, 6.0, 9.0, 7.5]

print(palavra[0])
print(palavra[3])
print(notas[0])
print(notas[-1])

# Desafio: o índice começa em 0 porque o computador armazena o endereço do início da sequência na memória. Cada índice representa um "deslocamento" a partir desse ponto — o primeiro elemento tem deslocamento 0.